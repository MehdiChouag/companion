<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.anyfetch.companion"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/MobileCompanionTheme">
        <service
            android:name=".commons.api.requests.HttpSpiceService"
            android:exported="false"/>
        <service
            android:name=".commons.android.requests.AndroidSpiceService"
            android:exported="false"/>

        <activity
            android:name=".AuthActivity"
            android:label="@string/title_activity_auth">
        </activity>
        <activity
            android:name=".UpcomingEventsActivity"
            android:label="@string/title_activity_upcoming_events">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".ContextActivity"
            android:label="@string/title_activity_context">
        </activity>

        <receiver
            android:name=".commons.notifications.MeetingPreparationAlarm"
            android:process=":remote"/>
        <receiver
            android:name=".notifications.MeetingPreparationNotification"
            android:exported="true">
            <intent-filter>
                <action android:name="com.anyfetch.companion.SHOW_NOTIFICATION"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".FullActivity"
            android:label="@string/title_activity_full">
        </activity>
    </application>

</manifest>
