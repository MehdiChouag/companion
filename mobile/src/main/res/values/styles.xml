<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="MobileCompanionTheme" parent="Theme.AppCompat.Light">
        <!-- NOTE: we can ignore compat warnings as we're in an AppCompat theme -->
        <item name="colorPrimary" tools:ignore="NewApi">@color/primary</item>
        <item name="colorPrimaryDark" tools:ignore="NewApi">@color/primary_dark</item>
        <item name="colorAccent">@color/color_accent</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor" tools:ignore="NewApi">@android:color/black</item>
        <item name="android:windowContentTransitions" tools:ignore="NewApi">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="PagerSlidingStyle">
        <item name="android:background">?colorPrimary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="pstsIndicatorColor">@android:color/white</item>
        <item name="pstsShouldExpand">true</item>
        <item name="pstsTextAllCaps">true</item>
        <item name="pstsDividerColor">@null</item>
    </style>

    <style name="AutoCompleteTextView" parent="Widget.AppCompat.Light.AutoCompleteTextView">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textCursorDrawable">@null</item>
    </style>

    <style name="searchTextHiglight">
        <item name="android:textStyle">bold</item>
    </style>

</resources>