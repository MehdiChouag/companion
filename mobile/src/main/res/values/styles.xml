<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="MobileCompanionTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- NOTE: we can ignore compat warnings as we're in an AppCompat theme -->
        <item name="android:colorPrimary" tools:ignore="NewApi">@color/primary</item>
        <item name="android:colorPrimaryDark" tools:ignore="NewApi">@color/primary_dark</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor" tools:ignore="NewApi">@android:color/black</item>

        <item name="android:windowContentTransitions" tools:ignore="NewApi">true</item>
        <item name="android:windowEnterTransition" tools:ignore="NewApi">@android:transition/slide_top</item>
        <item name="android:windowExitTransition" tools:ignore="NewApi">@android:transition/slide_bottom</item>
        <item name="android:windowSharedElementEnterTransition" tools:ignore="NewApi">
            @transition/change_image_transform
        </item>
        <item name="android:windowSharedElementExitTransition" tools:ignore="NewApi">
            @transition/change_image_transform
        </item>

    </style>
</resources>